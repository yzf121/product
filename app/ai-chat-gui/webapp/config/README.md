# Runtime AI Config

1. Copy `ai-config.template.js` to `ai-config.js`.
2. Fill `defaultApiKey` and `defaultAppId` (or assistant-specific values).
3. Keep `ai-config.js` out of git.

The app loads `config/ai-config.js` at runtime from `index.html`.

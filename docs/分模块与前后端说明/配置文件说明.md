# 配置文件说明

> **版本**: v1.4.3  
> **更新日期**: 2025-01-08

---

## 📋 配置文件概览

| 文件 | 用途 | 位置 |
|-----|------|------|
| `.env` | 环境变量配置 | 根目录 |
| `.cdsrc.json` | CDS 运行时配置 | 根目录 |
| `mta.yaml` | MTA 部署描述符 | 根目录 |
| `xs-security.json` | XSUAA 安全配置 | 根目录 |
| `package.json` | 项目配置 | 根目录 |
| `manifest.json` | UI5 应用描述符 | app/ai-chat-gui/webapp/ |
| `ui5.yaml` | UI5 本地配置 | app/ai-chat-gui/ |
| `ui5-deploy.yaml` | UI5 部署配置 | app/ai-chat-gui/ |
| `xs-app.json` | 应用路由配置 | app/ai-chat-gui/ |

---

## 1. 环境变量配置 (.env)

### 文件位置
- **模板**: `.env.example`
- **实际配置**: `.env`（不提交到版本控制）

### 配置项详解

```bash
# ==================== DashScope API 配置 ====================
# 阿里云百炼 API 密钥（必需）
# 获取方式：登录阿里云百炼控制台 -> API-Key 管理
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx

# 默认应用 ID（必需）
DASHSCOPE_APP_ID=xxxxxxxx

# ==================== 各 AI 助手应用 ID ====================
# 每个助手对应百炼平台上的一个应用
# 如未配置，将使用 DASHSCOPE_APP_ID 作为默认值

# ABAP Clean Core 助手
DASHSCOPE_APP_ID_ABAP=xxxxxxxx

# SAP CPI 集成助手
DASHSCOPE_APP_ID_CPI=xxxxxxxx

# 功能文档生成助手
DASHSCOPE_APP_ID_FUNC_DOC=xxxxxxxx

# 技术文档生成助手
DASHSCOPE_APP_ID_TECH_DOC=xxxxxxxx

# 代码审查助手
DASHSCOPE_APP_ID_CODE_REVIEW=xxxxxxxx

# 单元测试生成助手
DASHSCOPE_APP_ID_UNIT_TEST=xxxxxxxx

# 流程图生成助手
DASHSCOPE_APP_ID_DIAGRAM=xxxxxxxx

# ==================== 百炼 OpenAPI 配置 ====================
# 用于文件上传功能（可选）
# 注意：使用 RAM AccessKey，不是 DashScope API Key

# 阿里云 RAM AccessKey ID
BAILIAN_ACCESS_KEY_ID=LTAI5txxxxxxxxxx

# 阿里云 RAM AccessKey Secret
BAILIAN_ACCESS_KEY_SECRET=xxxxxxxxxx

# 百炼业务空间 ID
# 获取方式：百炼控制台 -> 业务空间 -> 空间 ID
BAILIAN_WORKSPACE_ID=llm-njsp72bv281ofywg
```

### ⚠️ RAM 权限配置（重要）

文件上传功能使用阿里云 RAM AccessKey 进行认证。如果遇到 **403 NoPermission** 错误，请按以下步骤检查权限配置：

#### 1. RAM 用户权限策略

需要为 RAM 用户授予百炼相关权限。登录 [RAM 控制台](https://ram.console.aliyun.com/)，为用户添加以下权限策略之一：

**方式 A: 使用系统策略（推荐）**
```
AliyunBailianFullAccess    # 百炼完整访问权限
```

**方式 B: 自定义策略（最小权限）**
```json
{
    "Version": "1",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "bailian:ApplyFileUploadLease",
                "bailian:AddFile",
                "bailian:DescribeFile"
            ],
            "Resource": "*"
        }
    ]
}
```

#### 2. 业务空间成员配置

登录 [百炼控制台](https://bailian.console.aliyun.com/)：
1. 进入业务空间 -> 空间管理 -> 成员管理
2. 点击"添加成员"
3. 选择 RAM 用户
4. 设置角色为 **管理员** 或 **开发者**

#### 3. 常见权限错误

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| `NoPermission: code: 403` | RAM 用户权限不足 | 添加 `AliyunBailianFullAccess` 策略 |
| `InvalidAccessKeyId` | AK 不存在或已禁用 | 检查 AccessKey 状态 |
| `Workspace not found` | 业务空间 ID 错误 | 检查 `BAILIAN_WORKSPACE_ID` |
| `User not in workspace` | 用户未加入空间 | 在百炼控制台添加成员 |

### 安全注意事项

⚠️ **重要**:
- `.env` 文件已添加到 `.gitignore`，切勿提交到版本控制
- 不要在日志中打印完整密钥
- 云端部署时通过 BTP Cockpit 配置环境变量

---

## 2. CDS 运行时配置 (.cdsrc.json)

### 文件位置
- 根目录: `.cdsrc.json`

### 配置内容

```json
{
    "requires": {
        "db": {
            "kind": "sql"
        }
    },
    "server": {
        "impl": "./srv/server.js"
    }
}
```

### 配置项说明

| 配置 | 说明 |
|-----|------|
| `requires.db.kind` | 数据库类型，开发时使用 SQLite |
| `server.impl` | 自定义服务器入口文件路径 |

### 扩展配置

如需添加更多配置：

```json
{
    "requires": {
        "db": {
            "kind": "sql"
        },
        "[production]": {
            "db": {
                "kind": "hana"
            }
        }
    },
    "server": {
        "impl": "./srv/server.js"
    },
    "log": {
        "levels": {
            "cds": "debug"
        }
    }
}
```

---

## 3. MTA 部署配置 (mta.yaml)

### 文件位置
- 根目录: `mta.yaml`

### 结构概览

```yaml
_schema-version: 3.3.0
ID: abap-clean-core-ai-fiori-new
description: AI 助手平台
version: 1.0.0

modules:          # 应用模块
  - name: ...     # 后端服务
  - name: ...     # 前端应用
  - name: ...     # 部署器

resources:        # 服务资源
  - name: ...     # XSUAA
  - name: ...     # Destination
  - name: ...     # HTML5 Repository

parameters:       # 全局参数
  deploy_mode: html5-repo
```

### 模块说明

#### 后端服务模块

```yaml
- name: abap-clean-core-ai-fiori-new-srv
  type: nodejs
  path: gen/srv
  requires:
    - name: abap-clean-core-ai-fiori-new-auth
  provides:
    - name: srv-api
      properties:
        srv-url: ${protocol}://${routes/0/route}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1          # 实例数量
    memory: 256M          # 内存配额
    random-route: true    # 随机路由
  build-parameters:
    builder: npm-ci
```

#### 前端应用模块

```yaml
- name: comaiassistantaichatapp
  type: html5
  path: app/ai-chat-gui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
      - npm install
      - npm run build:cf
    supported-platforms: []
```

#### HTML5 部署器

```yaml
- name: abap-clean-core-ai-fiori-new-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
    - name: abap-clean-core-ai-fiori-new-html5-repo-host
      parameters:
        content-target: true
  build-parameters:
    build-result: app/
    requires:
      - artifacts:
          - comaiassistantaichatapp.zip
        name: comaiassistantaichatapp
        target-path: app/
```

### 资源说明

#### XSUAA 服务

```yaml
- name: abap-clean-core-ai-fiori-new-auth
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json    # 安全配置文件
    service: xsuaa
    service-plan: application
```

#### Destination 服务

```yaml
- name: abap-clean-core-ai-fiori-new-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
            - Name: srv-api
              Type: HTTP
              URL: ~{srv-api/srv-url}
              Authentication: OAuth2UserTokenExchange
              HTML5.ForwardAuthToken: true
    service: destination
    service-plan: lite
```

#### HTML5 应用仓库

```yaml
- name: abap-clean-core-ai-fiori-new-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
```

---

## 4. 安全配置 (xs-security.json)

### 文件位置
- 根目录: `xs-security.json`

### 配置内容

```json
{
    "xsappname": "abap-clean-core-ai-fiori-new",
    "tenant-mode": "dedicated",
    "scopes": [
        {
            "name": "$XSAPPNAME.user",
            "description": "User access"
        }
    ],
    "role-templates": [
        {
            "name": "User",
            "description": "User role",
            "scope-references": [
                "$XSAPPNAME.user"
            ]
        }
    ],
    "role-collections": [
        {
            "name": "AIChat_User",
            "description": "AI Chat Application User",
            "role-template-references": [
                "$XSAPPNAME.User"
            ]
        }
    ],
    "oauth2-configuration": {
        "redirect-uris": [
            "https://*.cfapps.*.hana.ondemand.com/**",
            "https://*.launchpad.cfapps.*.hana.ondemand.com/**"
        ]
    }
}
```

### 配置项说明

| 配置 | 说明 |
|-----|------|
| `xsappname` | 应用名称，必须唯一 |
| `tenant-mode` | 租户模式：dedicated（专用）或 shared（共享） |
| `scopes` | 权限范围定义 |
| `role-templates` | 角色模板定义 |
| `role-collections` | 角色集合（用于分配给用户） |
| `oauth2-configuration` | OAuth2 配置，包括重定向 URI |

---

## 5. 项目配置 (package.json)

### 文件位置
- 根目录: `package.json`

### 主要配置

```json
{
    "name": "abap-clean-core-ai-fiori-new",
    "version": "1.0.0",
    "description": "AI 助手平台",
    
    "engines": {
        "node": ">=18.0.0"
    },
    
    "dependencies": {
        "@sap/cds": "^9",
        "@sap/xssec": "^4",
        "dotenv": "^16.3.1",
        "express": "^4",
        "multer": "^1.4.5-lts.1",
        "form-data": "^4.0.0",
        "@alicloud/bailian20231229": "^1.0.0",
        "@alicloud/openapi-client": "^0.4.0",
        "@alicloud/tea-util": "^1.4.0"
    },
    
    "devDependencies": {
        "@cap-js/sqlite": "^2.1.2",
        "@sap/cds-dk": "^9",
        "cds-plugin-ui5": "^0.13.0",
        "mbt": "^1.2.29",
        "rimraf": "^5.0.5"
    },
    
    "scripts": {
        "start": "cds-serve",
        "dev": "cds watch --open ai-chat-gui/webapp/index.html?sap-ui-xx-viewCache=false",
        "build": "rimraf resources mta_archives && mbt build --mtar archive",
        "deploy": "cf deploy mta_archives/archive.mtar --retries 1",
        "undeploy": "cf undeploy abap-clean-core-ai-fiori-new --delete-services --delete-service-keys --delete-service-brokers"
    },
    
    "cds": {
        "requires": {
            "[production]": {
                "auth": "xsuaa",
                "db": false
            },
            "destinations": true,
            "html5-repo": true
        }
    },
    
    "workspaces": [
        "app/*"
    ]
}
```

### 依赖说明

#### 生产依赖

| 包名 | 版本 | 用途 |
|-----|------|------|
| `@sap/cds` | ^9 | SAP CAP 核心框架 |
| `@sap/xssec` | ^4 | SAP BTP 安全库 |
| `dotenv` | ^16.3.1 | 环境变量加载 |
| `express` | ^4 | Web 框架 |
| `multer` | ^1.4.5-lts.1 | 文件上传处理 |
| `form-data` | ^4.0.0 | 表单数据处理 |
| `@alicloud/bailian20231229` | ^1.0.0 | 阿里云百炼 SDK |
| `@alicloud/openapi-client` | ^0.4.0 | 阿里云 OpenAPI 客户端 |
| `@alicloud/tea-util` | ^1.4.0 | 阿里云 SDK 工具库 |

#### 开发依赖

| 包名 | 版本 | 用途 |
|-----|------|------|
| `@cap-js/sqlite` | ^2.1.2 | 开发环境数据库 |
| `@sap/cds-dk` | ^9 | CDS 开发工具包 |
| `cds-plugin-ui5` | ^0.13.0 | UI5 集成插件 |
| `mbt` | ^1.2.29 | MTA 构建工具 |
| `rimraf` | ^5.0.5 | 跨平台文件删除 |

---

## 6. UI5 应用配置 (manifest.json)

### 文件位置
- `app/ai-chat-gui/webapp/manifest.json`

### 主要配置

```json
{
    "_version": "1.59.0",
    
    "sap.app": {
        "id": "com.ai.assistant.aichatapp",
        "type": "application",
        "title": "{{appTitle}}",
        "description": "{{appDescription}}",
        "applicationVersion": {
            "version": "1.0.0"
        },
        "i18n": "i18n/i18n.properties",
        "crossNavigation": {
            "inbounds": {
                "AIChat-Display": {
                    "semanticObject": "AIChat",
                    "action": "Display"
                }
            }
        }
    },
    
    "sap.cloud": {
        "public": true,
        "service": "abap-clean-core-ai-fiori-new"
    },
    
    "sap.ui": {
        "technology": "UI5",
        "deviceTypes": {
            "desktop": true,
            "tablet": true,
            "phone": true
        }
    },
    
    "sap.ui5": {
        "rootView": {
            "viewName": "com.ai.assistant.aichatapp.view.App",
            "type": "XML",
            "id": "app"
        },
        "dependencies": {
            "minUI5Version": "1.120.0",
            "libs": {
                "sap.m": {},
                "sap.ui.core": {},
                "sap.f": {},
                "sap.ui.layout": {}
            }
        },
        "models": {
            "i18n": {
                "type": "sap.ui.model.resource.ResourceModel",
                "settings": {
                    "bundleName": "com.ai.assistant.aichatapp.i18n.i18n",
                    "supportedLocales": ["", "en", "zh_CN"],
                    "fallbackLocale": ""
                }
            },
            "chat": {
                "type": "sap.ui.model.json.JSONModel",
                "settings": {
                    "data": {
                        "messages": [],
                        "conversations": [],
                        "currentConversationId": null,
                        "inputValue": "",
                        "isLoading": false,
                        "sidebarExpanded": true
                    }
                }
            }
        },
        "routing": {
            "config": {
                "routerClass": "sap.m.routing.Router",
                "viewType": "XML",
                "viewPath": "com.ai.assistant.aichatapp.view",
                "controlId": "app",
                "controlAggregation": "pages",
                "transition": "slide"
            },
            "routes": [
                {"name": "home", "pattern": "", "target": "home"},
                {"name": "chat", "pattern": "chat/{aiType}", "target": "chat"},
                {"name": "diagram", "pattern": "diagram", "target": "diagram"}
            ],
            "targets": {
                "home": {"viewName": "Home", "viewLevel": 1},
                "chat": {"viewName": "Main", "viewLevel": 2},
                "diagram": {"viewName": "Diagram", "viewLevel": 2}
            }
        },
        "resources": {
            "css": [{"uri": "css/style.css"}]
        }
    }
}
```

---

## 7. UI5 本地配置 (ui5.yaml)

### 文件位置
- `app/ai-chat-gui/ui5.yaml`

### 配置内容

```yaml
specVersion: "3.0"
metadata:
  name: comaiassistantaichatapp
type: application

framework:
  name: SAPUI5
  version: "1.120.0"
  libraries: []

builder:
  resources:
    excludes:
      - "**/test/**"
      - "**/localService/**"
```

---

## 8. UI5 部署配置 (ui5-deploy.yaml)

### 文件位置
- `app/ai-chat-gui/ui5-deploy.yaml`

### 配置内容

```yaml
# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json
specVersion: "3.0"
metadata:
  name: comaiassistantaichatapp
type: application

framework:
  name: SAPUI5
  version: "1.120.0"
  libraries: []

builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateVersionInfo
      configuration:
        archiveName: comaiassistantaichatapp
```

---

## 9. 应用路由配置 (xs-app.json)

### 文件位置
- `app/ai-chat-gui/xs-app.json`

### 配置内容

```json
{
    "welcomeFile": "/index.html",
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/api/(.*)$",
            "target": "/api/$1",
            "authenticationType": "xsuaa",
            "destination": "srv-api",
            "csrfProtection": false
        },
        {
            "source": "^(.*)$",
            "target": "$1",
            "authenticationType": "xsuaa",
            "service": "html5-apps-repo-rt"
        }
    ]
}
```

### 路由规则说明

| 规则 | 描述 |
|-----|------|
| `/api/*` | API 请求转发到后端服务 |
| `*` | 静态资源从 HTML5 仓库获取 |

---

## 10. 配置最佳实践

### 开发环境 vs 生产环境

| 配置项 | 开发环境 | 生产环境 |
|-------|---------|---------|
| 数据库 | SQLite | HANA / 无 |
| 认证 | 无 | XSUAA |
| 日志级别 | debug | info |
| 端口 | 4004 | 自动分配 |

### 配置优先级

1. 命令行参数
2. 环境变量 (`.env`)
3. 配置文件 (`.cdsrc.json`)
4. 默认值

### 敏感信息处理

- ✅ 使用环境变量存储敏感信息
- ✅ 使用 `.env.example` 作为模板
- ✅ 将 `.env` 添加到 `.gitignore`
- ❌ 不要在代码中硬编码密钥
- ❌ 不要在日志中打印完整密钥
